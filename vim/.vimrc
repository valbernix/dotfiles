syntax on " syntax highlighting
set tabstop=2 " tab width
set shiftwidth=4 " number of spaces for ai
set expandtab " use space chars instead of tabs
set ai " auto indenting
set number " shows line number on the left-hand side
set history=1000 " commands history; defaults to 20

highlight Visual ctermbg=blue guibg=LightBlue " sets the background color of visual mode to blue
highlight String ctermfg=Cyan guifg=Cyan " sets string color to cyan
highlight Comment ctermfg=green " sets the comment color to green

